import { Flex } from "antd";
import styled from "styled-components";

export const CardWrapper = styled(Flex)`
  width: 100%;

  .cover,
  .body {
    width: 100%;
  }

  .body {
    padding: 16px;
  }
`;
